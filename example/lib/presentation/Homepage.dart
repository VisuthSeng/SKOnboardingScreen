import 'package:flutter/material.dart';

class HomeA extends StatelessWidget {
  const HomeA({key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Home Screen"),
      ),
    );
  }
}
